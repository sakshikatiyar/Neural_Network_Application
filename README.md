# XOR Neural Network (NumPy Only)

This project implements a simple feedforward neural network in pure NumPy to solve the XOR problem — a classic benchmark for nonlinear modeling.

## 🔧 Requirements
- Python 3.x
- NumPy
- Matplotlib
